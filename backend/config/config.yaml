server:
  port: 8005
  environment: development

database:
  url: "postgres://bootx:bootx@localhost:5432/bootx"

redis:
  url: "redis://localhost:6380"

auth:
  jwt_expiry_hours: 24
  refresh_token_expiry_days: 30

app:
  orchestrator: docker
  gpu_enabled: false
  frontend_url: "http://localhost:3000"

warmpool:
  enabled: true
